// const trimWhiteSpace = (doc) => {
//   Object.keys(doc).forEach((key) => {
//     if (doc.isModified(key) && typeof doc[key] === "string") {
//       doc[key] = doc[key].replace(/\s+/g, " ").trim();
//     }
//     console.log(doc[key]);
//   });
// };

// export default trimWhiteSpace;
